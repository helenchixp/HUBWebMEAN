/*
angular.module('HUBWebApp.contact.detail', ['ui.route'])
    .component('contactDetail', {
        template: 'He is {{$ctrl.no}}',
        controller: [
            function ContactDetailController($stateParams) {
                this.no = $stateParams.no;
            }
        ]
    });
*/